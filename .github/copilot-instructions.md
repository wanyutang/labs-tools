# Copilot Instructions

Use `AGENTS.md` as the canonical project and harness guide.

This is a static GitHub Pages app. Preserve browser-only token handling, PWA
compatibility, mobile safe-area layout, and `npm run build` as the deploy
validation path.

Do not introduce committed secrets, backend services, paid services, or global
configuration changes.
